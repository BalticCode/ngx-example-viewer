import { HighlightLoaderPipe } from './highlight-loader.pipe';

describe('HighlightLoaderPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightLoaderPipe();
    expect(pipe).toBeTruthy();
  });
});
